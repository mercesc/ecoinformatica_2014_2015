## Reto_3_p3: Algoritmo que haga la media de 10 temperaturas indicadas por el usuario

t <- scan (n=10) # Listado de 10 temperaturas a introducir por el usuario.

media <- mean (t,trim=0) # Se hace la media de las temperaturas.

print (media) # Muestra el resultado de la media.

